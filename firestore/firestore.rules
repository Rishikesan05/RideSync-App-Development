rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /route_fares/{routeId} {
      allow read: if resource.data.active == true;
      allow write: if false;
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
